pub mod event;
pub mod function;
pub mod handler;
pub mod inngest;
pub mod result;
pub mod router;
pub mod sdk;

pub fn add(left: usize, right: usize) -> usize {
    left + right
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn it_works() {
        let result = add(2, 2);
        assert_eq!(result, 4);
    }
}
