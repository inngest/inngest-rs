#+AUTHOR: Darwin D. Wu
#+DATE: 2026-04-16
#+STATUS: Done

* Milestone 000: Protocol Conformance Baseline

** Goal
- Bring the current HTTP SDK into basic wire-level compliance with the spec before adding more behavior.

** Scope
- Response headers and versions
- Sync payload versioning and shape
- Step runtime URL/query parameter correctness
- Error response structure and retry headers

** Checklist
- [X] Update `X-Inngest-Sdk` formatting to match `name:vsemver`
- [X] Update `X-Inngest-Req-Version` to `2` for all relevant responses
- [X] Fix sync payload `v` from `1` to `0.1`
- [X] Ensure sync payload `sdk` matches the response header format exactly
- [X] Fix step runtime URL generation to use `stepId=step` instead of `step=step`
- [X] Ensure the top-level sync URL does not accidentally include deploy-specific query parameters
- [X] Align call error payloads to the spec shape `{ name, message, stack? }`
- [X] Return `X-Inngest-No-Retry: true|false` on all call error responses
- [X] Return `400` for non-retriable errors and `500` for retriable errors
- [X] Preserve `Retry-After` behavior only for retriable errors
- [X] Add tests for sync payload serialization
- [X] Add tests for HTTP response headers and status codes

** Exit Criteria
- [X] Sync requests and call responses serialize to spec-compatible shapes
- [X] Header/version behavior is covered by tests
- [X] No known wire-format mismatches remain in the base HTTP path

** Out of Scope
- Step execution model changes
- Full payload retrieval
- Parallelism
- Connect, streaming, checkpointing
